# Module 5 — Transaction history

| Test Case ID | Test Description | Preconditions | Test Steps | Expected Result | Actual Result | Priority | Automation Feasibility |
|---|---|---|---|---|---|---|---|
| TC-TXN-001 | A deposit is recorded as Credit | Logged in as a customer | 1. Deposit a known amount (e.g. 77).<br>2. Open "Transactions". | A row exists with type "Credit", amount 77, and a non-empty timestamp. | Pass — automated (2026-09-06) | High | Automated — `tests/07-transactions.spec.ts` ("TC-TXN-001") |
| TC-TXN-002 | A withdrawal is recorded as Debit | Logged in as a customer with sufficient balance | 1. Withdraw a known amount (e.g. 35).<br>2. Open "Transactions". | A row exists with type "Debit", amount 35, and a non-empty timestamp. | **Fail (2026-09-06) — known live-app flake.** The live app's Transactions view intermittently renders empty right after a deposit+withdraw combo in the same session, even with a built-in retry. Not a test-authoring defect; re-run to confirm before treating as newly broken. | High | Automated — `tests/07-transactions.spec.ts` ("TC-TXN-002") |
| TC-TXN-003 | Reset behaves consistently with what is actually shown | Logged in as a customer with at least one transaction | 1. Open "Transactions".<br>2. If a "Reset" control (inside the date-range filter panel) is visible, click it and check the table.<br>3. If not visible, check the table without using it. | If Reset is reachable, using it clears the transaction list. If it is not reachable, the transaction history remains intact and visible. | Pass — automated (2026-09-06) | Low | Automated — `tests/07-transactions.spec.ts` ("TC-TXN-003") |
| TC-TXN-004 | Back button returns to the account page | On the Transactions page | 1. Click "Back". | URL returns to the account route; the balance is displayed correctly again. | Pass — automated (2026-09-06) | Medium | Automated — `tests/07-transactions.spec.ts` ("TC-TXN-004") |
| TC-TXN-005 | Transaction order and timestamps are consistent | Logged in as a customer | 1. Deposit 100.<br>2. Withdraw 30.<br>3. Deposit 20.<br>4. Open "Transactions" and read the last 3 rows. | Rows appear in the order performed (Credit 100, Debit 30, Credit 20); each has a non-empty timestamp; timestamps are non-decreasing in that order. | Pass — automated (2026-09-06). Shares the same known live-app flake as TC-TXN-002 (see its note) — can occasionally need a re-run. | High | Automated — `tests/07-transactions.spec.ts` ("TC-TXN-005") |
| TC-TXN-006 | An account with a large transaction volume loads without error | Customer/account with an extensive seeded transaction history (e.g. Hermoine Granger, account 1001) | 1. Log in.<br>2. Open "Transactions". | The table renders all rows (100+) without error, timeout, or pagination cutoff that hides data. | Pass — automated (2026-09-06) | Medium | Automated — `tests/07-transactions.spec.ts` ("TC-TXN-006") |
| TC-TXN-007 | Transaction dates are readable and use a consistent format | Transactions view open | 1. Read the timestamp column across several rows. | All timestamps use the same format/timezone convention; none are ambiguous, truncated, or rendered as raw epoch values. | Not executed — pending manual QA | Low | Manual only — visual/formatting review |
| TC-TXN-008 | Transaction table has meaningful column headers | Transactions view open | 1. Inspect the table header row. | Columns are clearly labelled (e.g. Date, Amount, Type) so a user can interpret each row without guessing. | Not executed — pending manual QA | Low | Manual only — usability review |
| TC-TXN-009 | Transactions are specific to the selected account (TXN-06) | Customer with multiple accounts (e.g. Hermoine Granger) | 1. Deposit a distinctive amount on account A.<br>2. Switch to account B.<br>3. Open "Transactions" for account B. | Account B's transaction history does not contain account A's deposit; no cross-account leakage. | Pass — automated (2026-09-06) | Medium | Automated — `tests/07-transactions.spec.ts` (the code's own test name is "TC-TXN-007: transactions are specific to the selected account" — coincides numerically with the different, manual-only TC-TXN-007 above; assigned this catalog's next free ID instead) |
| TC-TXN-010 | An account with no transactions shows an empty history (TXN-07) | A freshly opened account with no transactions yet | 1. Open a new account via Bank Manager.<br>2. Log in and select the new account.<br>3. Open "Transactions". | The table renders with zero rows; no error, no stale data from another account. | Pass — automated (2026-09-06) | Low | Automated — `tests/07-transactions.spec.ts` (the code's own test name is "TC-TXN-008: an account with no transactions shows an empty history" — coincides numerically with the different, manual-only TC-TXN-008 above; assigned this catalog's next free ID instead) |
