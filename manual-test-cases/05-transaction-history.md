# Module 5 — Transaction history

| Test Case ID | Test Description | Preconditions | Test Steps | Expected Result | Actual Result | Priority | Automation Feasibility |
|---|---|---|---|---|---|---|---|
| TC-TXN-001 | A deposit is recorded as Credit | Logged in as a customer | 1. Deposit a known amount (e.g. 77).<br>2. Open "Transactions". | A row exists with type "Credit", amount 77, and a non-empty timestamp. | Pass — automated | High | Automated — `tests/transactions.spec.ts` (TC-TXN-001) |
| TC-TXN-002 | A withdrawal is recorded as Debit | Logged in as a customer with sufficient balance | 1. Withdraw a known amount (e.g. 35).<br>2. Open "Transactions". | A row exists with type "Debit", amount 35, and a non-empty timestamp. | **Fail (last run) — likely flaky.** The live app's Transactions view intermittently renders empty right after a deposit+withdraw combo in the same session; re-run to confirm before filing as a defect. | High | Automated — `tests/transactions.spec.ts` (TC-TXN-002) |
| TC-TXN-003 | Reset behaves consistently with what is actually shown | Logged in as a customer with at least one transaction | 1. Open "Transactions".<br>2. If a "Reset" control (inside the date-range filter panel) is visible, click it and check the table.<br>3. If not visible, check the table without using it. | If Reset is reachable, using it clears the transaction list. If it is not reachable, the transaction history remains intact and visible. | Pass — automated | Low | Automated — `tests/transactions.spec.ts` (TC-TXN-003) |
| TC-TXN-004 | Back button returns to the account page | On the Transactions page | 1. Click "Back". | URL returns to the account route; the balance is displayed correctly again. | Pass — automated | Medium | Automated — `tests/transactions.spec.ts` (TC-TXN-004) |
| TC-TXN-005 | Transaction order and timestamps are consistent | Logged in as a customer | 1. Deposit 100.<br>2. Withdraw 30.<br>3. Deposit 20.<br>4. Open "Transactions" and read the last 3 rows. | Rows appear in the order performed (Credit 100, Debit 30, Credit 20); each has a non-empty timestamp; timestamps are non-decreasing in that order. | **Fail (last run) — likely flaky.** Same intermittent empty-table issue as TC-TXN-002; re-run to confirm. | High | Automated — `tests/transactions.spec.ts` (TC-TXN-005) |
| TC-TXN-006 | An account with a large transaction volume loads without error | Customer/account with an extensive seeded transaction history (e.g. Hermoine Granger, account 1001) | 1. Log in.<br>2. Open "Transactions". | The table renders all rows (100+) without error, timeout, or pagination cutoff that hides data. | Pass — automated | Medium | Automated — `tests/transactions.spec.ts` (TC-TXN-006) |
| TC-TXN-007 | Transaction dates are readable and use a consistent format | Transactions view open | 1. Read the timestamp column across several rows. | All timestamps use the same format/timezone convention; none are ambiguous, truncated, or rendered as raw epoch values. | Not executed — pending manual QA | Low | Manual only — visual/formatting review |
| TC-TXN-008 | Transaction table has meaningful column headers | Transactions view open | 1. Inspect the table header row. | Columns are clearly labelled (e.g. Date, Amount, Type) so a user can interpret each row without guessing. | Not executed — pending manual QA | Low | Manual only — usability review |
