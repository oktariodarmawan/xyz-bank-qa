# Module 3 — Deposit functionality

| Test Case ID | Test Description | Preconditions | Test Steps | Expected Result | Actual Result | Priority | Automation Feasibility |
|---|---|---|---|---|---|---|---|
| TC-DEP-001 | A valid deposit increases the balance | Logged in as a customer; starting balance known | 1. Click "Deposit".<br>2. Enter a positive whole amount (e.g. 100).<br>3. Submit. | A success message ("Deposit Successful") appears; balance increases by exactly the deposited amount. | Pass — automated | High | Automated — `tests/deposit.spec.ts` (TC-DEP-001) |
| TC-DEP-002 | Empty amount is not processed | Deposit form open | 1. Leave the Amount field empty.<br>2. Submit. | No success message appears; balance is unchanged. | Pass — automated | High | Automated — `tests/deposit.spec.ts` (TC-DEP-002) |
| TC-DEP-003 | Deposit of 0 leaves the balance unchanged | Deposit form open | 1. Enter 0 in Amount.<br>2. Submit. | No success message appears; balance is unchanged. | Pass — automated | High | Automated — `tests/deposit.spec.ts` (TC-DEP-003) |
| TC-DEP-004 | Negative deposit amount is rejected | Deposit form open | 1. Enter -50 in Amount.<br>2. Submit. | No success message appears; balance is unchanged (does not decrease). | Pass — automated | High | Automated — `tests/deposit.spec.ts` (TC-DEP-004) |
| TC-DEP-005 | Decimal deposit amount is rejected by the Amount field | Deposit form open | 1. Enter 100.50 in Amount.<br>2. Submit. | Observed rule: the Amount field only accepts whole numbers, so a decimal value fails field validation and no transaction is recorded; balance unchanged. | Pass — automated | Medium | Automated — `tests/deposit.spec.ts` (TC-DEP-005) |
| TC-DEP-006 | Non-numeric characters are rejected | Deposit form open | 1. Type "abc" into the Amount field.<br>2. Submit. | The Amount field cannot hold non-numeric text (remains empty); no success message; balance unchanged. | Pass — automated | Medium | Automated — `tests/deposit.spec.ts` (TC-DEP-006) |
| TC-DEP-007 | A very large deposit amount is calculated correctly | Deposit form open | 1. Enter a very large whole number (e.g. 99,999,999,999).<br>2. Submit. | Balance increases by exactly the entered amount, with no overflow, rounding, or truncation error. | Pass — automated | Medium | Automated — `tests/deposit.spec.ts` (TC-DEP-007) |
| TC-DEP-008 | Excessively large deposit amount is rejected | Deposit form open | 1. Enter an unreasonably large amount (e.g. 999,999,999,999).<br>2. Submit. | **Expected per test plan:** the deposit is rejected with no success message and an unchanged balance. **Observed on the live app:** the deposit succeeds — there is no upper-bound validation. | **Fail — confirmed application gap.** Success message appeared and the balance increased. | High | Automated — `tests/validation.spec.ts` (documents the gap; assertion currently fails by design) |
| TC-DEP-009 | Deposit success message is clear and does not disappear prematurely | Valid deposit submitted | 1. Submit a valid deposit.<br>2. Time how long the confirmation stays visible/readable. | The success message is legible and remains on screen long enough to be read, not just flashed. | Not executed — pending manual QA | Low | Manual only — subjective/timing-based UX check |
