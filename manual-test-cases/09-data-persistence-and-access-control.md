# Module 9 — Data persistence, integrity, and unauthorised access

| Test Case ID | Test Description | Preconditions | Test Steps | Expected Result | Actual Result | Priority | Automation Feasibility |
|---|---|---|---|---|---|---|---|
| TC-PERS-001 | Customer data persists after a page refresh | Logged in as a customer; a deposit just made | 1. Log in, select an account, deposit a known amount.<br>2. Record the resulting balance.<br>3. Refresh the page (F5).<br>4. Re-check the welcome message, selected account, balance, and Transactions. | Customer identity, selected account, balance, and the just-made transaction all remain exactly as before the refresh. | **Fail — confirmed application gap** (2026-09-06). After refresh, the customer stays logged in on the same account, but balance and transaction history reset to the seed data (the deposit just made is lost). | High | Automated — `tests/11-access-and-persistence.spec.ts` (documents the gap; assertion currently fails by design) |
| TC-PERS-002 | Customer data persists after logout and re-login | Logged in as a customer; a deposit just made | 1. Deposit a known amount and note the resulting balance.<br>2. Click "Logout".<br>3. Log back in as the same customer and reselect the same account.<br>4. Check balance and Transactions. | Balance and the transaction history recorded before logout are still present after re-login. | Pass — automated (2026-09-06) | High | Automated — `tests/11-access-and-persistence.spec.ts` |
| TC-PERS-003 | A customer session cannot reach manager controls by direct navigation | Logged in as a customer | 1. While logged in as a customer, change the URL directly to the manager route.<br>2. Check for Add Customer / Open Account / Customers controls. | Manager-only controls remain hidden even via direct URL navigation. | Pass — automated (2026-09-06), consistently across every run this session. **Resolved:** the source code previously carried a comment claiming this was a known route-guard gap; that comment was inaccurate (never reproduced) and has been corrected in `tests/11-access-and-persistence.spec.ts`. The app does enforce this route guard. | High | Automated — `tests/11-access-and-persistence.spec.ts` |
| TC-PERS-004 | A manager session does not expose another customer's private account data | Navigated as Bank Manager (no customer bound to this session) | 1. While in the manager session, navigate directly to the customer account route (`#/account`). | No real account number or balance renders for an unbound manager session (no other customer's private data leaks through). | Pass — automated (2026-09-06) | High | Automated — `tests/11-access-and-persistence.spec.ts` |
| TC-PERS-005 | Deposit and withdrawal both update the balance consistently across a session | Logged in as a customer | 1. Deposit 100.<br>2. Withdraw 40.<br>3. Compare the running balance shown after each step against manual arithmetic. | Balance after each operation exactly matches starting balance + deposits − withdrawals, with no drift. | Pass — automated (2026-09-06) | High | Automated — `tests/14-banking.spec.ts` |
| TC-PERS-006 | No data inconsistency when switching between two different customers in the same browser | No active session | 1. Log in as Customer A, note balance.<br>2. Log out.<br>3. Log in as Customer B, deposit a known amount.<br>4. Log out.<br>5. Log back in as Customer A. | Customer A's balance after step 5 matches step 1 (unaffected by Customer B's session in between); no cross-contamination of account data. | Pass — automated (2026-09-06) | Medium | Automated — `tests/11-access-and-persistence.spec.ts` ("no data inconsistency when switching between two different customers") |
| TC-PERS-007 | A manager-added customer persists after a page refresh (DATA-02) | Navigated as Bank Manager | 1. Add a new customer.<br>2. Confirm it appears in the Customers list.<br>3. Refresh the page.<br>4. Re-check the Customers list. | The newly added customer is still present after the refresh (unlike balance/transaction data — see TC-PERS-001 — customer records themselves persist). | Pass — automated (2026-09-06) | Medium | Automated — `tests/11-access-and-persistence.spec.ts` ("a newly added customer persists after a page refresh") |
