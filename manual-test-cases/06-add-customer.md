# Module 6 — Bank Manager: Add Customer

| Test Case ID | Test Description | Preconditions | Test Steps | Expected Result | Actual Result | Priority | Automation Feasibility |
|---|---|---|---|---|---|---|---|
| TC-ADD-001 | Add a customer with valid data | Logged in / navigated as Bank Manager, on Add Customer | 1. Enter a unique First Name and Last Name.<br>2. Enter a numeric Post Code.<br>3. Click "Add Customer". | A confirmation dialog states the customer was added successfully with a new customer ID; the customer appears in the Customers list. | Pass — automated | High | Automated — `tests/add-customer.spec.ts` (TC-ADD-001) |
| TC-ADD-002 | All fields empty is not submitted | Add Customer form open, all fields empty | 1. Click "Add Customer" without filling any field. | Required-field validation blocks submission; no confirmation dialog appears. | Pass — automated | High | Automated — `tests/add-customer.spec.ts` (TC-ADD-002) |
| TC-ADD-003 | Missing Post Code is not submitted | First Name and Last Name filled, Post Code empty | 1. Fill First Name and Last Name.<br>2. Leave Post Code empty.<br>3. Click "Add Customer". | Required-field validation blocks submission; no confirmation dialog appears. | Pass — automated | High | Automated — `tests/add-customer.spec.ts` (TC-ADD-003) |
| TC-ADD-004 | An exact duplicate customer is rejected | A customer with a known first name, last name, and post code already exists | 1. Add a customer with a unique name/post code (baseline).<br>2. Submit the exact same first name, last name, and post code again. | Second submission is rejected with a "may be duplicate" message; the customer is not added twice (list still shows one row). | Pass — automated | Medium | Automated — `tests/add-customer.spec.ts` (TC-ADD-004) |
| TC-ADD-005 | Name with numbers and special characters is accepted | Add Customer form open | 1. Enter a first/last name containing digits and symbols (e.g. `J0hn@#`).<br>2. Fill a valid Post Code.<br>3. Submit. | Observed rule: name fields accept any characters — submission succeeds and the customer appears in the list. | Pass — automated | Low | Automated — `tests/add-customer.spec.ts` (TC-ADD-005) |
| TC-ADD-006 | Post Code with letters is accepted | Add Customer form open | 1. Fill valid name fields.<br>2. Enter a Post Code with letters (e.g. `ABCDE`).<br>3. Submit. | Observed rule: Post Code accepts alphanumeric input; submission succeeds. (See TC-ADD-009 — this is the same underlying gap as rejecting an *invalid* post code.) | Pass — automated | Low | Automated — `tests/add-customer.spec.ts` (TC-ADD-006) |
| TC-ADD-007 | A very long name is handled without breaking the layout | Add Customer form open | 1. Enter a first name of ~300 characters.<br>2. Fill the remaining fields validly.<br>3. Submit. | Submission succeeds; the Customers table still renders correctly afterwards (no layout break). | Pass — automated | Low | Automated — `tests/add-customer.spec.ts` (TC-ADD-007) |
| TC-ADD-008 | Leading/trailing spaces are trimmed from the name | Add Customer form open | 1. Enter a first name and last name padded with spaces (e.g. `"  Bob  "`).<br>2. Submit.<br>3. Check the Customers list. | The stored name is trimmed of leading/trailing whitespace. | Pass — automated | Low | Automated — `tests/add-customer.spec.ts` (TC-ADD-008) |
| TC-ADD-009 | Invalid (non-numeric) Post Code is rejected | Add Customer form open | 1. Fill a unique, valid name.<br>2. Enter a non-numeric Post Code (e.g. `abcde`).<br>3. Submit. | **Expected per test plan:** submission is rejected with a validation message; customer is not created. **Observed on the live app:** the customer is created successfully — Post Code format is not validated. | **Fail — confirmed application gap.** Confirmation dialog reported success and the customer appeared in the list. | High | Automated — `tests/validation.spec.ts` (documents the gap; assertion currently fails by design) |
| TC-ADD-010 | Validation messages are clear when a required field is missing | Add Customer form partially filled | 1. Leave one required field empty.<br>2. Attempt to submit.<br>3. Read the browser/field-level validation feedback. | The message clearly identifies which field is missing/invalid, is visible, and does not require guessing. | Not executed — pending manual QA | Low | Manual only — subjective UX/copy review |
