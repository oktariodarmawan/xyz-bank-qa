# Module 8 — Bank Manager: Customers list, search, sort, delete

This is the "data display, search and filtering" surface referenced in the scope overview. It
was automated but was missing from the original `TEST-PLAN.md` table — added here for full
coverage.

| Test Case ID | Test Description | Preconditions | Test Steps | Expected Result | Actual Result | Priority | Automation Feasibility |
|---|---|---|---|---|---|---|---|
| TC-CUST-001 | The customer list is fully displayed | Navigated as Bank Manager, on Customers | 1. Open "Customers".<br>2. Inspect the table header and first row. | Table shows First Name, Last Name, Post Code, Account Number, and Delete columns; at least one row with a working "Delete" button is present. | Pass — automated | Medium | Automated — `tests/customers.spec.ts` (TC-CUST-001) |
| TC-CUST-002 | Search filters the table by name | Customers list open | 1. Type a known first name (e.g. "Harry") into the search box. | The table narrows to only rows matching "Harry"; the visible row(s) contain that text. | Pass — automated | High | Automated — `tests/customers.spec.ts` (TC-CUST-002) |
| TC-CUST-003 | Search with no matches shows an empty table, not an error | Customers list open | 1. Type a string that matches no customer (e.g. "zzzzz"). | Table shows zero rows; no error or broken layout. | Pass — automated | Medium | Automated — `tests/customers.spec.ts` (TC-CUST-003) |
| TC-CUST-004 | Search is not case-sensitive | Customers list open | 1. Search "harry" (lower case) and note the row count.<br>2. Search "HARRY" (upper case) and note the row count. | Both searches return the same number of matching rows (greater than zero). | Pass — automated | Low | Automated — `tests/customers.spec.ts` (TC-CUST-004) |
| TC-CUST-005 | Deleting a customer removes their row | A disposable test customer exists in the list | 1. Locate the customer's row.<br>2. Click "Delete" on that row. | The row is removed from the table immediately, without a page reload. | Pass — automated | High | Automated — `tests/customers.spec.ts` (TC-CUST-005) |
| TC-CUST-006 | A deleted customer disappears from the Customer Login dropdown | A test customer was just deleted (TC-CUST-005) | 1. Go to Customer Login.<br>2. Open the "Your Name" dropdown. | The deleted customer's name is no longer listed as an option. | Pass — automated | Medium | Automated — `tests/customers.spec.ts` (TC-CUST-006) |
| TC-CUST-007 | Sorting by the First Name column works both directions | Customers list open, multiple rows present | 1. Click the "First Name" column header once; record the order.<br>2. Click it again; record the order. | One click produces alphabetically ascending order, the other produces descending order; the two orders differ from each other. | Pass — automated | Medium | Automated — `tests/customers.spec.ts` (TC-CUST-007) |
| TC-CUST-008 | A newly added customer appears in the list | Add Customer flow completed for a new customer | 1. Add a customer with a unique name.<br>2. Navigate to Customers. | The new customer's row is present without needing a manual page refresh. | Pass — automated | Medium | Automated — `tests/customers.spec.ts` (TC-CUST-008) |
| TC-CUST-009 | Search box has a clear, discoverable label/placeholder | Customers list open | 1. Look at the search input without typing anything. | The input's purpose (searching/filtering customers) is obvious from its label or placeholder text, not just its position on the page. | Not executed — pending manual QA | Low | Manual only — usability/discoverability review |
