# Module 2 — Customer dashboard and account overview

Covers the account information panel and switching between a customer's accounts.

| Test Case ID | Test Description | Preconditions | Test Steps | Expected Result | Actual Result | Priority | Automation Feasibility |
|---|---|---|---|---|---|---|---|
| TC-ACC-001 | Account information panel is fully displayed | Logged in as a customer (e.g. Harry Potter) | 1. Log in.<br>2. Read the "Account Number", "Balance", and "Currency" labels and values. | Account Number is a numeric value; Balance is numeric (integer or decimal); Currency shows one of Dollar/Pound/Rupee. | Pass — automated | High | Automated — `tests/account-view.spec.ts` (TC-ACC-001) |
| TC-ACC-002 | Switching account updates account number, balance, and currency together | Logged in as a customer with multiple accounts (e.g. Hermoine Granger: 1001, 1002, 1003) | 1. Select account 1002 from the dropdown.<br>2. Record the account number, balance, currency.<br>3. Select account 1003.<br>4. Record the same fields again. | After each selection, the account number matches the one chosen, and balance/currency update together — no value carried over from the previous account. | Pass — automated | High | Automated — `tests/account-view.spec.ts` (TC-ACC-002) |
| TC-ACC-003 | Logout ends the customer session | Logged in as a customer | 1. Click "Logout". | URL returns to the customer login route; the "Your Name" selector is visible again (i.e. no longer authenticated). | Pass — automated | High | Automated — `tests/account-view.spec.ts` (TC-ACC-003) |
| TC-ACC-004 | Welcome message shows the correct customer name | Logged in as a customer | 1. Log in as a specific customer.<br>2. Read the welcome text. | Welcome text contains the exact name of the logged-in customer, not another customer's name. | Pass — automated | Medium | Automated — `tests/banking.spec.ts`, `tests/customer-login.spec.ts` |
| TC-ACC-005 | Default account selected on login is the customer's first account | Customer with multiple accounts | 1. Log in.<br>2. Read the initially-selected value in the Account dropdown. | The first account (e.g. 1001 for Hermoine Granger) is selected by default, without requiring a manual selection. | Pass — automated | Medium | Automated — `tests/banking.spec.ts` |
| TC-ACC-006 | Account overview values are numeric and free of formatting glitches | Logged in as a customer | 1. Inspect the Balance field across a few accounts.<br>2. Check for stray characters, wrong decimal separators, or truncation. | Balance renders as a clean number (optionally with a decimal portion); no currency symbol/locale mismatch, no truncated digits. | Not executed — pending manual QA | Low | Manual only — visual/formatting review |
